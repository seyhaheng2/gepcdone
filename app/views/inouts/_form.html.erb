<%= form_for(@inout) do |f| %>
  <% if @inout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inout.errors.count, "error") %> prohibited this inout from being saved:</h2>

      <ul>
      <% @inout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
